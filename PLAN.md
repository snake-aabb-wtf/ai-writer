# AI Writer：自动化小说生产 Agent 集群计划

## 1. 项目定位

AI Writer 是一个**单用户、多小说的自动化创作工作台**，使用 JavaScript/TypeScript 构建。

系统目标：用户提供一句话灵感、故事梗概或完整设定后，多个 Agent 协作完成世界观构建、剧情规划、章节生产、连贯性检查和持续连载。

第一版只负责**自动生成和保存小说内容**，不自动发布到外部平台。

## 2. 已确定的产品原则

- 小说类型由 WebUI 自定义，系统不把题材写死。
- 支持三种启动输入：一句话灵感、故事梗概、完整设定。
- 目标是全自动连载，默认不需要人工逐章审核。
- 连载按故事阶段推进，不按固定日历机械生成。
- 总纲动态演化：核心主题和终局方向可以作为锚点，具体剧情随故事状态调整。
- 章节粒度由系统根据复杂度自动选择：章节级或场景级。
- 篇幅按剧情自然决定，不强制固定字数。
- 剧情逻辑优先，设定一致性和文风一致性其次，其他质量指标尽力保证。
- 文风通过自然语言描述配置。
- 人物允许死亡、永久离场和重大变化；一旦发生就成为不可违背的故事事实。
- 伏笔由系统自动登记、分级、推进、回收和检查长期悬置。
- 用户未提供的世界观、人物和设定，Agent 可以自主创造。
- 已生成的过去正文**不可修改**，后续剧情负责对历史问题进行自洽。
- 结构化状态不覆盖旧记录，只能通过追加更正保持可追溯性。
- 当前章节多次修订仍失败时，保留最佳版本、记录问题并继续推进。
- 运行中的 WebUI 第一版提供暂停/继续，不直接修改过去正文、设定或未来大纲。
- 每部小说可以独立配置生成语言。

## 3. 总体架构

```text
WebUI
  ↓
Node.js/TypeScript API
  ↓
工作流编排器 / 总控 Agent
  ├─ 固定核心 Agent
  │   ├─ 输入分析与设定 Agent
  │   ├─ 故事规划 Agent
  │   ├─ 章节/场景写作 Agent
  │   ├─ 连贯性检查 Agent
  │   ├─ 伏笔与时间线 Agent
  │   └─ 章节整理 Agent
  │
  └─ 动态临时 Agent
      └─ 根据特殊剧情任务临时创建，由总控授权权限
  ↓
任务队列 + 共享故事状态库
  ↓
不可修改正文时间线 + 追加式结构化事实记录
```

### 3.1 模型调用层

第一版只配置一个 OpenAI Chat Completions 兼容的 API 和模型，所有 Agent 共用：

```env
OPENAI_BASE_URL=https://example.com/v1
OPENAI_API_KEY=...
OPENAI_MODEL=...
```

Agent 的差异来自：

- 角色提示词
- 输入上下文
- 可使用的工具
- 读写权限
- 所处工作流阶段

暂不实现多模型路由。后续需要时再抽象模型适配层。

### 3.2 Agent 组织方式

- 固定核心 Agent 负责稳定主流程。
- 动态 Agent 负责特殊剧情任务，例如某类谜题设计、战斗编排、专业知识补全或特定文风检查。
- 动态 Agent 默认只读。
- 总控 Agent 根据任务临时授予具体权限。
- Agent 不直接无限互聊，主要通过任务队列和结构化状态库协作。

### 3.3 协作方式

采用混合模式：

- 任务通过队列流转。
- 故事事实通过共享状态库维护。
- 总控 Agent 负责拆任务、汇总结果、授权写入和推进状态。
- 所有写入都必须经过 Schema 校验和状态版本检查。

## 4. 核心工作流

### 4.1 创建小说

```text
创建项目
→ 选择生成语言
→ 输入题材和文风描述
→ 输入一句话灵感 / 故事梗概 / 完整设定
→ 输入分析 Agent 判断信息完整度
→ 设定 Agent 补全缺失内容
→ 生成初始人物、世界观、时间线和核心主题
→ 生成动态总纲与第一个故事阶段
→ 开始自动生产
```

### 4.2 故事阶段生产

```text
读取当前故事阶段
→ 评估阶段目标和复杂度
→ 决定章节级或场景级生产
→ 生成章节/场景计划
→ 检索相关人物、设定、时间线和伏笔
→ 写作 Agent 生成草稿
→ 连贯性与设定检查
→ 不合格则定向修订
→ 达到重试上限则保留最佳版本并记录问题
→ 写入不可修改正文
→ 追加人物状态、伏笔和时间线事件
→ 判断故事阶段是否完成
→ 完成则动态规划下一阶段
```

### 4.3 复杂度评估

复杂度评估器至少考虑：

- 当前场景涉及的人物数量
- 同时推进的剧情线数量
- 伏笔和未解决问题数量
- 时间线跨度
- 世界观规则引用数量
- 是否存在人物重大变化
- 是否需要特殊 Agent

复杂度低时可以章节级生成；复杂度高时拆分为多个场景。

## 5. 故事状态模型

每部小说拥有独立的 Story Runtime 和状态空间。

### 5.1 必备实体

- `Project`：小说项目配置、语言、题材、文风、运行状态。
- `StoryBible`：世界观、规则、势力、地点和核心主题。
- `Character`：人物设定、动机、关系、当前状态和不可逆事件。
- `Outline`：动态总纲、故事阶段、章节目标和场景目标。
- `TimelineEvent`：已发生的时间线事件，只追加不覆盖。
- `Foreshadowing`：伏笔、重要性、首次出现、推进状态和回收状态。
- `Chapter`：章节元数据、标题、摘要、正文版本和关联状态。
- `Scene`：章节内部场景、目标、冲突和结果。
- `Correction`：对历史结构化记录的追加更正。
- `Task`：Agent 任务、输入快照、输出产物、重试次数和状态。

### 5.2 不可变规则

- 已生成正文只能追加，不能覆盖或删除。
- 已发生的时间线事件只能追加更正，不能直接改写。
- 人物死亡、永久离场等不可逆事件必须成为后续上下文的一部分。
- 未来大纲可以重新规划，但不能伪造过去没有发生的事件。
- 每次 Agent 写入都保存输入快照和来源任务。

### 5.3 章节完成后的自动保存

每章至少保存：

1. 标题和摘要
2. 人物状态
3. 伏笔状态和时间线事件

质量检查结果可以作为任务日志保存，暂不作为第一版的核心章节产物。

## 6. 数据与目录规划

建议项目目录：

```text
AI Writer/
├─ apps/
│  ├─ api/                 # HTTP API
│  ├─ worker/              # 队列消费者和 Agent 执行器
│  └─ web/                 # WebUI
├─ packages/
│  ├─ domain/              # 领域类型和状态机
│  ├─ agents/              # 固定 Agent 与动态 Agent 工厂
│  ├─ workflows/           # 工作流定义
│  ├─ prompts/             # 版本化提示词
│  ├─ tools/               # 检索、校验、写入工具
│  ├─ model-provider/      # OpenAI 兼容接口
│  └─ storage/             # 数据访问层
├─ data/
│  ├─ projects/            # 小说项目数据
│  ├─ drafts/              # 草稿和中间产物
│  └─ exports/             # 导出文件
├─ tests/
├─ .env.example
├─ package.json
└─ README.md
```

MVP 可以使用 SQLite 保存元数据、状态和任务，正文与快照保存为 Markdown/JSON。后续再根据规模迁移 PostgreSQL 和 Redis。

## 7. WebUI 规划

### 7.1 首页

- 多小说项目列表
- 当前故事阶段和生成进度
- 运行/暂停状态
- 当前 Agent 与任务队列
- 错误和重试信息
- 最近生成章节

### 7.2 小说工作台

- 正文阅读和章节导航
- 世界观、人物、时间线、伏笔视图
- 当前剧情目标
- 历史状态和追加更正记录
- 任务运行日志
- 暂停/继续控制

### 7.3 创建项目表单

- 小说名称
- 生成语言
- 自定义小说类型
- 文风自然语言描述
- 一句话灵感 / 故事梗概 / 完整设定
- 自动生成开关

## 8. 技术路线

### MVP 推荐

- Node.js + TypeScript
- Fastify 或 NestJS
- SQLite
- Redis + BullMQ
- Zod
- Markdown + JSON
- React/Vite WebUI

### 重要实现要求

- Agent 输出必须使用结构化 JSON Schema 校验。
- 所有任务必须有明确状态：`pending`、`running`、`succeeded`、`failed`、`paused`。
- 任务必须支持重试、超时、幂等和断点恢复。
- 写入故事状态前必须进行版本检查。
- 不允许 Agent 直接任意读写文件系统。
- 所有提示词必须版本化，便于复现和排查。
- 所有生成结果保留来源模型、提示词版本和输入快照。

## 9. 分阶段实施计划

### Phase 0：项目骨架（已完成）

- 初始化 Node.js/TypeScript 项目
- 加载 `.env`
- 实现 OpenAI Chat Completions 兼容客户端
- 定义领域类型、任务类型和基础 Schema

### Phase 1：单部小说最小闭环（已完成）

- 创建小说项目
- 输入一句话灵感
- 生成初始设定和人物
- 生成一个故事阶段
- 生成一章正文
- 保存标题、摘要、人物状态、伏笔和时间线

### Phase 2：工作流与队列（已完成基础能力）

- 引入可替换的持久化任务队列；当前 MVP 使用 SQLite，BullMQ/Redis 作为后续 transport
- 固定核心 Agent
- 任务重试、超时和暂停/继续
- 章节复杂度评估
- 章节级/场景级自动选择

### Phase 3：连贯性与动态演化（已完成基础能力）

- 故事事实检索
- 设定/人物/时间线一致性检查
- 伏笔状态与长期悬置检测
- 故事阶段显式完成判断
- 下一阶段动态规划

### Phase 4：WebUI 工作台（已完成）

- 多小说项目管理
- 进度和运行状态
- 正文阅读
- 人物、时间线、伏笔视图
- 暂停/继续

### Phase 5：动态 Agent 与稳定性（已完成）

- 动态 Agent 工厂：临时 Agent 有独立生命周期和过期时间
- 总控授权机制：权限只允许从请求集合中授予，默认无写权限
- 临时 Agent 任务隔离：独立任务 ID、授权绑定和 `isolationKey`
- 失败降级和最佳版本保留：重试/最终失败均保留 `bestOutput`
- 完整事件日志：任务创建、领取、重试、暂停、成功、失败、恢复和 Agent 授权均持久化
- 乐观状态版本检查和恢复测试

## 10. 第一版验收标准

- 可以在 WebUI 创建多个小说项目。
- 可以用三种不同详细程度的输入启动项目。
- 可以使用 `.env` 中配置的 OpenAI 兼容 API 生成内容。
- 系统能自动生成设定、人物、故事阶段和章节。
- 系统能根据复杂度选择章节级或场景级流程。
- 已生成正文不会被后续任务覆盖。
- 人物状态、伏笔和时间线可以在后续任务中被正确读取。
- 发生历史矛盾时，只能追加更正，不能修改过去正文。
- 当前章节失败达到上限后可以保留最佳版本并继续推进。
- 可以暂停和继续运行中的小说项目。
- 进程重启后任务能够恢复，不需要从头生成整部小说。
- 动态 Agent 不能越权访问或修改未授权的故事状态。
- 任务失败时当前最佳输出不会丢失，且关键生命周期事件可追溯。

## 11. 当前明确不做

- 第一版不自动发布到外部平台。
- 第一版不接入多模型路由。
- 第一版不允许修改过去正文。
- 第一版不做多人协作和权限系统。
- 第一版不追求几十个 Agent；先验证固定核心 Agent + 动态临时 Agent 的闭环。
- 第一版不强制固定章节字数。
